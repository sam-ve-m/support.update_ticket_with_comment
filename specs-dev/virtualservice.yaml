apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: update-ticket-fn-vs
  namespace: fission
spec:
  hosts:
    - dev.api.siga.me
  gateways:
    - develop/dev-istio-gateway
  http:
  - match:
    - uri:
        prefix: /router/support/update-ticket
    rewrite:
      uri: /support/update-ticket
    route:
    - destination:
        host: router.fission.svc.cluster.local
        port:
          number: 80